#include <stdio.h>
#include <stdlib.h>
#include<string.h>
//strlen tinh do dai 1 chuoi dem den khi gap null thi ngung

int main()
{
    char str[]="AnhBa";
    int dodai=strlen(str);
    printf("%d",dodai);
    return 0;
}
